/** Circuit Archive design system: signal cards make public alumni records legible, warm, and actionable. */
import {Link} from "wouter"; import {ArrowUpRight,MapPin} from "lucide-react"; import type {Alumni} from "@/data/alumni";
export default function AlumniCard({person}:{person:Alumni}){return <article className="alumni-card"><div className="alumni-card__image"><img src={person.photo} alt={`Portrait of ${person.name}`}/><span className="signal-dot"/></div><div className="alumni-card__content"><div className="card-kicker">BATCH {person.batch}</div><h3>{person.name}</h3><p className="alumni-card__role">{person.position}</p><p className="alumni-card__organization">{person.organization}</p><div className="alumni-card__bottom"><span><MapPin size={14}/>{person.district}</span><Link href={`/alumni/${person.slug}`} className="text-link">View profile <ArrowUpRight size={15}/></Link></div></div></article>}
