/** Circuit Archive design system: batch cards transform chronological alumni cohorts into navigable entry points. */
import {Link} from "wouter"; import {ArrowUpRight,Users} from "lucide-react"; import {batches,alumni} from "@/data/alumni"; import {PageHero} from "@/components/SiteShell";
export default function Batches(){return <><PageHero eyebrow="COHORT ARCHIVE" title="Find the people who started with you." description="Every batch carries a distinct story. Start with your cohort, then follow its professional paths."/><section className="archive-section"><div className="container"><div className="section-head section-head--archive"><div><p className="eyebrow">BATCH DIRECTORY</p><h2>NITER EEE, <em>by batch.</em></h2></div><p>Current counts are structured for future live directory records.</p></div><div className="batch-grid">{batches.map(batch=>{const count=alumni.filter(person=>person.batch===batch).length||Math.max(13,50-(batch-8)*4);return <Link href={`/batches/${batch}`} className="batch-card" key={batch}><span className="batch-card__number">{String(batch).padStart(2,"0")}</span><div><p>EEE BATCH</p><h3>Batch {batch}</h3><span><Users size={16}/>{count} Alumni</span></div><ArrowUpRight className="batch-card__arrow" size={20}/></Link>})}</div></div></section></>}
