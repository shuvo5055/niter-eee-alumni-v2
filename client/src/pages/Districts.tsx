/** Circuit Archive design system: geographic navigation gives the national alumni network a calm, searchable surface. */
import {useMemo,useState} from "react"; import {Link} from "wouter"; import {ArrowUpRight,Search} from "lucide-react"; import {districts,alumni} from "@/data/alumni"; import {PageHero} from "@/components/SiteShell";
export default function Districts(){const[query,setQuery]=useState("");const found=useMemo(()=>districts.filter(district=>district.toLowerCase().includes(query.toLowerCase())),[query]);return <><PageHero eyebrow="GEOGRAPHIC NETWORK" title="A department connected across Bangladesh." description="Locate NITER EEE alumni through the districts that define their roots, community, and professional reach."/><section className="district-section"><div className="container"><div className="district-tools"><div><p className="eyebrow">64 DISTRICTS</p><h2>Start with a <em>place.</em></h2></div><label className="district-search"><Search size={19}/><input value={query} onChange={event=>setQuery(event.target.value)} placeholder="Search District"/></label></div><div className="district-grid">{found.map(district=>{const count=alumni.filter(person=>person.district===district).length;return <Link href={`/districts/${encodeURIComponent(district)}`} className="district-card" key={district}><span>{district}</span><small>{count?`${count} alumni listed`:"View district"}</small><ArrowUpRight size={18}/></Link>})}</div></div></section></>}
